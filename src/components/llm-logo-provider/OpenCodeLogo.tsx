const OpenCodeLogo = ({ className = 'w-5 h-5' }) => {
  return (
    <div className={`${className} flex items-center justify-center bg-green-500 text-white text-xs font-bold rounded`}>
      OC
    </div>
  );
};

export default OpenCodeLogo;
